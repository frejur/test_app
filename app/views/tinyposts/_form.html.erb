<%= form_for(@tinypost) do |f| %>
  <% if @tinypost.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tinypost.errors.count, "error") %> prohibited this tinypost from being saved:</h2>

      <ul>
      <% @tinypost.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :text %><br />
    <%= f.text_field :text %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
